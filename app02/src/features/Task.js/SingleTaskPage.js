import React from 'react'
import { useSelector } from 'react-redux'
import { selectTask } from '../allTasks/allTasksSlice.js';

export const SingleTaskPage = () => {
    // const task = useSelector(selectTask(taskId));

    // console.log(task);

    return (
        <div> single task page! </div>
    )
}